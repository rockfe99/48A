[ web4 - 방명록 예제 ]

SpringBoot, JPA, MySQL, Thymeleaf
HTML, CSS, JavaScript

- 글 쓰기
- 전체 글 보기
- 삭제
- 수정

* 저장날짜의 자동 처리
1. 환경설정 클래스 또는 main() 포함된 클래스에
    @EnableJpaAuditing
2. Entity 클래스에
    @EntityListeners(AuditingEntityListener.class)
3. 날짜/시간 변수에
    @CreatedDate


[ web4 예제에 추가 ]
작성자의 IP를 글 정보에 추가
추천기능

-- 테이블 수정 SQL
ALTER TABLE guestbook
ADD COLUMN recommend INT DEFAULT 0,
ADD COLUMN ip VARCHAR(30);

[ 그 외의 기능 추가 ]
추천
작성자 IP 저장
중복 추천 금지
신고기능
+ 신고 수가 일정 값을 넘으면 자동 삭제 또는 블라인드
IP차단 (글쓰기 금지 또는 특정 경로 진입 금지)





